"""Streamlit entrypoint.

Streamlit Community Cloud examples often refer to `streamlit_app.py`.
We keep the real app in `app.py` and import it here so either path works.
"""

import app  # noqa: F401
